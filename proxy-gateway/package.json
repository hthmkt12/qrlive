{
  "name": "proxy-gateway",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node --watch src/server.mjs",
    "start": "node src/server.mjs",
    "check": "node -e \"import('./src/server.mjs').then(({ createProxyGatewayServer }) => { if (typeof createProxyGatewayServer !== 'function') process.exit(1); console.log('proxy-gateway ok'); })\"",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "dotenv": "^16.6.1",
    "http-proxy-agent": "^7.0.2",
    "https-proxy-agent": "^7.0.6",
    "socks-proxy-agent": "^8.0.5"
  }
}
